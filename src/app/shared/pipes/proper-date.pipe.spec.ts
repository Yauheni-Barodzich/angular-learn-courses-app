import { ProperDatePipe } from './proper-date.pipe';

describe('ProperDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ProperDatePipe();
    expect(pipe).toBeTruthy();
  });
});
